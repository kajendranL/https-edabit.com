
#You hired three programmers and you (hopefully) pay them. Create a function that takes three numbers 
#(the hourly wage of each programmer) and returns the difference between the highest-paid programmer and the lowest-paid.

def programmers(one, two, three):
	diff = one,two,three
	return max(diff) - min(diff)




print(programmers(147, 33, 526)) # 493
print(programmers(33, 72, 74)) # 41
print(programmers(1, 5, 9)) # 8
